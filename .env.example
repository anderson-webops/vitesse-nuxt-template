# Local API listener. Production container defaults are defined in Dockerfile.
HOST=127.0.0.1
PORT=3006
TRUST_PROXY_HOPS=0

# Nuxt development proxy target. The browser still calls the same-origin /api path.
DEV_API_ORIGIN=http://127.0.0.1:3006
